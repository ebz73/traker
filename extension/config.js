const DEV = true; // flip to false for production builds
const API_BASE_URL = DEV ? "http://localhost:8000" : "https://traker-api.azurewebsites.net";
const WEB_APP_URL = DEV ? "http://localhost:5173" : "https://traker.azurewebsites.net";
